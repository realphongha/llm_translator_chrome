# LLM Page Translator v0.1.0

*A Chrome extension for translating webpages using any OpenAI-compatible API.*

---

## Goals

- Lightweight and fast
- Works with any OpenAI-compatible endpoint
- Supports local models (llama.cpp, Ollama, LM Studio, etc.)
- Supports cloud providers (OpenRouter, OpenAI, DeepSeek, etc.)
- Paragraph-aware translation
- Optimized for web novels (Qidian) while remaining general-purpose
- No account system
- No backend server
- Everything stored locally
- Event-driven architecture

---

## Features Implemented

### Core Pipeline

```
DOM
  │
  ├─ TreeWalker text extraction
  ├─ Priority computation via CSS rules
  │
  ▼
Enqueue → Background via chrome.runtime.sendMessage
  │
  ├─ Priority sort (lower number = higher priority)
  ├─ Parallel workers (configurable, default 32)
  ├─ Cache check (SHA-256 key, true LRU eviction)
  ├─ Batch builder (group by maxChars, split oversized)
  │
  ▼
API Call (POST /v1/chat/completions)
  │
  ├─ Retry with exponential backoff (1s, 2s, 4s)
  ├─ Parse <ID=N> tagged response
  ├─ Merge split paragraphs
  │
  ▼
Content Script
  │
  ├─ Apply translations to DOM
  ├─ State indicators (⟳ translating / ✓ translated / ⚠ error)
  └─ Stuck element recovery (2s after queue empties)
```

---

### Global Settings

Stored in `chrome.storage.local` under key `"global_config"`.

#### API Section

| Field | Type | Default | Notes |
|---|---|---|---|
| API Base URL | `string` | `""` | e.g. `http://localhost:11434/v1` |
| API Key | `string` | `""` | Bearer token, empty for local models |
| Model | `string` | `""` | e.g. `llama3`, `gpt-4o-mini` |
| Parallel Calls | `number` | `32` | Max concurrent API requests |
| Timeout | `number` | `60` | Request timeout in seconds |
| Temperature | `number` (optional) | `0.1` | Sampling temperature |
| Top-P | `number` (optional) | — | Nucleus sampling |
| Top-K | `number` (optional) | — | Top-K sampling |
| Min-P | `number` (optional) | — | Min-P sampling |
| Presence Penalty | `number` (optional) | — | Token presence penalty |
| Chat Template Kwargs | `Record<string,unknown>` (optional) | `{"enable_thinking":false}` | Kwargs for chat template |

#### Translation Section

| Field | Type | Default | Notes |
|---|---|---|---|
| Max Characters Per Request | `number` | `4000` | Paragraphs batched up to this limit |
| Retry Count | `number` | `3` | Retries with exponential backoff |

#### Cache Section

| Field | Type | Default | Notes |
|---|---|---|---|
| Max Cache Size | `number` | `7` MB | LRU eviction (true LRU, configurable 1-100 MB) |

---

### Translation Cache

```
Key:
  SHA-256(systemPrompt + "\x00" + model + "\x00" + text)

Value:
  { translation: string, timestamp: number }

Eviction:
  - True LRU (reads update recency order in index)
  - Hard count cap: 10,000 entries
  - Byte-based cap: configurable (default 7 MB, via chrome.storage.local.getBytesInUse)
  - Oldest entries evicted first (front of index)

Storage:
  chrome.storage.local with "cache_" prefix
```

---

### Per-Site Settings

Stored in `chrome.storage.local` under key `"site_<hostname>"`. Auto-created on first visit.

| Field | Type | Default | Notes |
|---|---|---|---|
| Enabled | `boolean` | `false` | Translation on/off for this site |
| Prompt | `string` | `"general"` | ID of built-in or user prompt |
| Source Language | `string` | `"Auto"` | Source language name |
| Target Language | `string` | `"English"` | Target language name |
| Observe | `string` | `"body"` | CSS selector for MutationObserver root |
| Content Selector | `string` | `""` | CSS selector for paragraphs (empty = auto) |
| Ignore | `string[]` | `[]` | CSS selectors for ignored elements |
| Priority Rules | `PriorityRule[]` | `[]` | CSS selector → priority number |

---

### Built-in Site Presets

| Site | Prompt | Source → Target | Selector | Priority |
|---|---|---|---|---|
| qidian.com | Qidian VN | Chinese → Vietnamese | `.article-wrapper p` | priority 1 |
| m.qidian.com | Qidian VN | Chinese → Vietnamese | `.read-content p` | priority 1 |
| www.novelupdates.com | General | Auto → Vietnamese | auto | — |
| fanyi.baidu.com | General | Auto → English | auto | — |
| All others | General | Auto → English | auto | — |

---

### Built-in Prompts

#### General
- **ID:** `"general"`
- **Description:** General-purpose translation for any language pair
- **Template:**
  ```
  You are a professional translator.
  Translate from {{source_language}} to {{target_language}}.
  Rules:
  - Preserve formatting.
  - Preserve paragraph structure.
  - Do not summarize.
  - Do not explain.
  - Output only the translation.
  ```

#### Qidian VN
- **ID:** `"qidian-vn"`
- **Description:** Optimized for Chinese web novels translated to Vietnamese
- **Template:**
  ```
  You are translating a Chinese web novel into natural Vietnamese.
  Rules:
  - Preserve the original tone.
  - Character names use Han-Viet (Hán Việt) readings as the base.
  - Sect names, technique names, and cultivation terms follow established conventions.
  - Existing Vietnamese fan translations of popular works should be referenced.
  - Use standard cultivation terminology.
  - Dialogue self-reference (我): "ta" — every character, always.
  - Dialogue address (你): "ngươi" — every character, always.
  - Narration: "hắn" (male), "nàng" (female), "nó" (neutral/pejorative).
  - Relationship vocatives: Han-Viet (sư phụ, sư huynh, etc.)
  - Preserve paragraph structure.
  - Keep dialogue natural.
  - Do not summarize.
  - Do not explain.
  - Output only the translation.
  ```

**Template variables:** `{{source_language}}`, `{{target_language}}`, `{{hostname}}`, `{{url}}`, `{{page_title}}`

Users can duplicate built-in prompts and create custom ones.

---

### User Prompt (shared template)

Builds the user-turn message with `<ID=N>` tagged paragraphs:

```
Translate the following paragraphs.

Each paragraph begins with an ID.

Return one translated paragraph for each ID.

<ID=1>
...
<ID=2>
...
<ID=3>
...
```

---

### Popup UI

```
┌─────────────────────────────────────────────┐
│  🔤 LLM Translator              [ON]         │
├─────────────────────────────────────────────┤
│  SITE     qidian.com                        │
│  PROMPT   [Qidian VN          ▼]            │
│  LANGUAGE [Chinese ▼] → [Vietnamese ▼]      │
├─────────────────────────────────────────────┤
│  12 In queue          Running               │
├─────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  │ Translate│ │Retranslat│ │  Pause   │     │
│  └──────────┘ └──────────┘ └──────────┘     │
│                                             │
│  Enable on this site  [═══════○]             │
│                                             │
│  ▼ Selectors & Priority (3 rules)           │
│    Content Selector: [.article p        ]   │
│    Ignore Selectors: [.ads, .comments   ]   │
│    Priority Rules:                          │
│      .article-wrapper p  →  1  [×]         │
│      .sidebar           → 50  [×]         │
│      [+ Add Rule]                          │
│    [Scan this page]                        │
│                                             │
├─────────────────────────────────────────────┤
│  [Clear Cache]   │   [⚙ Settings]          │
└─────────────────────────────────────────────┘
```

Width: 520px. Collapsible "Selectors & Priority" section. Scan results shown as cards with one-click "Use as Content" / "Ignore" / "Priority N" buttons.

---

### Options Page

5 sections via sidebar navigation:

#### 1. API
- Base URL, API Key (show/hide toggle), Model
- Parallel Calls (1-32), Timeout (5-300s)
- Temperature (0-2), Top-P (0-1), Top-K (0-200), Min-P (0-1), Presence Penalty (-2 to 2)
- Chat Template Kwargs (JSON text input)
- Test Connection button + Save button

#### 2. Translation
- Max Characters Per Request (500-50000)
- Retry Count (0-10)
- Save button

#### 3. Prompts
- Built-in prompts listed as cards with "Duplicate" button
- Custom prompts with "Edit" / "Delete" buttons
- "New Prompt" button opens editor panel
- Editor: name input, system prompt textarea with template variable hint, Save/Cancel

#### 4. Site Profiles
- List of all saved site configs as clickable cards
- Editor panel: enabled/disabled, prompt selector, language pair, observe/selector/ignore fields
- Save/Cancel/Delete buttons

#### 5. Cache
- Cached entries count (display)
- Storage used (formatted as B/KB/MB/GB)
- Max Cache Size (1-100 MB, default 7)
- Save Cache Settings + Clear Cache buttons

---

### DOM Observer

- `MutationObserver` with 400ms debounce
- Observes `childList: true, subtree: true` on configured root (default `document`)
- SPA navigation: `popstate`, `hashchange`, patched `history.pushState`/`replaceState`
- 800ms delay after URL change before re-scanning entire body
- 300ms initial delay for client-side framework render

### Text Extractor

- Walks text nodes via `TreeWalker` inside matching scope elements
- Skips: `<script>`, `<style>`, `<textarea>`, `<input>`, `<select>`, `<button>`, `<svg>`, `<canvas>`, `<code>`, `<pre>`, `<kbd>`, `<samp>`, `<math>`, `<head>`, `<link>`, `<meta>`, `<title>`
- Skips elements matching ignore selectors (checks ancestors via `el.closest()`)
- Skips already-translated elements (`[data-translation-id]`)
- Strips trailing spinner character (` ⟳`)
- Skips empty text nodes and non-letter text
- Wraps each text node in `<span data-translation-id="N" data-original="..." data-translation-state="waiting">`
- Computes priority via `el.closest()` against priority rules (lowest matching wins)

### Priority System

- Lower number = higher priority (1 = highest, default = `Infinity`)
- Items sorted by priority, then by enqueue order (FIFO)
- Priority rules defined per-site: `{ selector: string, priority: number }`
- Used in both extraction (sets `data-priority`) and queue (sorting)

### State Indicators (CSS)

- `"translating"` → spinning ⟳ icon, blue color, 0.7 opacity
- `"translated"` → clean (no indicator), full opacity
- `"error"` → ⚠ icon, red color, wavy underline
- `"waiting"` → no indicator

---

### DOM Scan (Popup Debug Feature)

Groups DOM elements by CSS path (4 levels deep, stops at `#id`). Heuristic:

| Count | Suggestion | Priority |
|---|---|---|
| ≥15 | "content" (likely main content) | 1 |
| 5-14 | "priority" (moderate frequency) | 50 |
| <5 | "ignore" (likely nav/UI) | 99 |

One-click buttons: "Use as Content" → sets content selector, "Ignore" → adds to ignore list, "Priority N" → adds priority rule.

---

### Languages Supported

23 languages: Auto Detect, Chinese (Simplified), Chinese (Traditional), English, Vietnamese, Japanese, Korean, French, German, Spanish, Portuguese, Russian, Arabic, Hindi, Thai, Indonesian, Malay, Italian, Dutch, Polish, Turkish, Swedish, Czech, Ukrainian.

---

### Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        Chrome Extension                     │
│                                                             │
│  ┌─────────────────────┐         ┌──────────────────────┐   │
│  │   Content Script    │◄────────│  Background Worker   │   │
│  │                     │ messages │                      │   │
│  │  observer.ts        │────────►│  queue.ts (priority  │   │
│  │  extractor.ts       │  ENQUEUE│  + parallel pool)    │   │
│  │  renderer.ts        │◄────────│  api.ts (HTTP calls) │   │
│  │                     │ RESULTS │  cache.ts (LRU)      │   │
│  └─────────────────────┘         └──────────┬───────────┘   │
│                                             │               │
│  ┌─────────────────────┐         ┌──────────▼───────────┐   │
│  │   Popup (popup.ts)  │         │   Options Page       │   │
│  │   Status, controls, │         │   All settings,      │   │
│  │   DOM scan          │         │   prompts, sites,    │   │
│  └─────────────────────┘         │   cache management   │   │
│                                  └──────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              chrome.storage.local                    │   │
│  │  global_config │ site_<hostname> │ cache_<sha256>    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

### Key Design Decisions

1. **No streaming API** — Simple request/response reduces complexity. Results are still progressive via per-sub-batch emission.

2. **IIFE bundles** — Chrome extension CSP restrictions make IIFE the safe choice over ESM for content scripts.

3. **Priority-based queue** — Items sorted on every enqueue. Processing is parallel but highest-priority items always start first (sorted to front of queue).

4. **True LRU cache** — Every cache hit updates recency order (key moved to end of index). Dual eviction: hard count cap (10K) + byte-based cap (default 7 MB).

5. **Retry logic** — 3 retries with exponential backoff (1s, 2s, 4s). HTTP 401 is not retried (auth error).

6. **Stuck element recovery** — Elements stuck in "translating" state are reverted and re-enqueued 2s after queue empties.

7. **Config auto-reload** — Content script listens to `chrome.storage.onChanged` and re-initializes automatically.

---

### Build System

| Tool | Version | Purpose |
|---|---|---|
| esbuild | 0.21.5 | Bundle TypeScript → IIFE |
| TypeScript | 5.5.4 | Type checking |
| sharp | 0.35.3 | Icon processing |
| @types/chrome | 0.0.270 | Chrome API types |

**Entry points:** background, content, popup, options (4 bundles)

**Output:** `dist/` directory (gitignored)

**Scripts:** `build` (minified), `dev` (watch + sourcemaps), `clean`

---

### Development Roadmap

#### Phase 1 (Complete)
- Manifest V3
- API client with configurable sampling parameters
- Popup with status and controls
- Global settings (API, translation, cache)
- Simple paragraph translation

#### Phase 2 (Complete)
- Batch builder with paragraph splitting
- Parallel worker pool (32 concurrent default)
- LRU cache with byte-based eviction
- Retranslate

#### Phase 3 (Complete)
- Site profiles with per-site config
- DOM rules (observe/selector/ignore/priority)
- MutationObserver with debounce
- SPA support (history API, popstate, hashchange)

#### Phase 4 (Complete)
- Prompt editor with duplicate built-ins
- Custom prompts
- DOM scan (popup debug tool)
- Advanced sampling parameters (temperature, top_p, top_k, min_p, presence_penalty)
- Disable thinking (chat_template_kwargs)

#### Phase 5 (Future)
- Translation memory (term consistency across pages)
- Glossary (user-defined term mappings injected into prompt)
- Hover original (show original text on hover)
- Export/import settings
- Keyboard shortcuts (Alt+T translate, Alt+R retranslate)
